# ---------------------------------------------------------------
# Solana Development .gitignore
# This file helps Git ignore temporary, build, and sensitive files
# generated during Solana development.
# ---------------------------------------------------------------

# ---------------------------------------------------------------
# 1. Node.js / JavaScript / TypeScript (for client-side/tests)
# ---------------------------------------------------------------
node_modules/
dist/
build/
.parcel-cache/
.env
.env.*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json # Often ignored if using yarn.lock or pnpm-lock.yaml
yarn.lock         # Can be ignored if using package-lock.json or pnpm-lock.yaml
pnpm-lock.yaml    # Can be ignored if using package-lock.json or yarn.lock

# ---------------------------------------------------------------
# 2. Rust / Cargo (for Solana programs)
# ---------------------------------------------------------------
target/
Cargo.lock # Often committed for reproducible builds, but can be ignored if desired
             # (e.g., in library crates where consumers manage dependencies)

# ---------------------------------------------------------------
# 3. Solana CLI / Local Validator
# ---------------------------------------------------------------
# Local validator ledger data
ledger/
test-ledger/
.solana/
.cache/solana/

# Keypairs (CRITICAL: NEVER commit real keypairs, especially for mainnet!)
# It's good practice to ignore any directory that might contain keypairs.
# For development, you might have `keypairs/my-dev-wallet.json` etc.
keypairs/
*.json # Be careful with this if you have JSON files you *do* want to track.
       # Consider more specific patterns if needed, e.g., `!src/**/*.json`
       # or `!config/*.json` if you have config JSONs.

# Program logs generated by the local validator
program-logs/

# ---------------------------------------------------------------
# 4. Anchor Framework Specific
# ---------------------------------------------------------------
.anchor/
artifacts/ # This directory contains compiled IDLs and program binaries.
            # Usually regenerated on `anchor build`.

# ---------------------------------------------------------------
# 5. Operating System Specific
# ---------------------------------------------------------------
.DS_Store       # macOS
Thumbs.db       # Windows
ehthumbs.db     # Windows
Desktop.ini     # Windows
.localized      # macOS

# ---------------------------------------------------------------
# 6. Editor / IDE Specific
# ---------------------------------------------------------------
.vscode/        # Visual Studio Code settings (can be committed if shared settings are desired, but often ignored)
.idea/          # IntelliJ IDEA / WebStorm
.project        # Eclipse
.classpath      # Eclipse
.settings/      # Eclipse
.cproject       # Eclipse CDT
*.sublime-workspace # Sublime Text
*.sublime-project   # Sublime Text
.history/       # VS Code Local History
*.swp           # Vim swap files
*~              # Emacs backup files
*.bak           # Generic backup files
*.orig          # Merge conflict backup files

# ---------------------------------------------------------------
# 7. Miscellaneous
# ---------------------------------------------------------------
# Log files
*.log

# Temporary files
*.tmp
*.temp

# VS Code Live Share
.vsls/

# Any files ending with .un~ (as discussed previously)
*.un~
